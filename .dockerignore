# Everything the image does not need. Keeping this list short and explicit
# matters more than completeness: a build context that carries node_modules is
# slow on every build, and .env carries secrets that must never reach a layer.
node_modules
.next
.git
.env
.env.*
!.env.example
measurements.jsonl
screenshots
tests
docs
*.md
